--[[

    __init Runtime Router

    Griffin Dalby
    2026.1.22

        This routes the __init behavior from the runtime module.

--]]

local runtime = require(script.Parent.packet.runtime)
return runtime